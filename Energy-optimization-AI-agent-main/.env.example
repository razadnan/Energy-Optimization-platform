# Copy this file to .env and fill in your key to enable the InsightAgent's
# LLM reasoning step. Without a key, InsightAgent automatically falls back
# to a deterministic rule-based summary - the pipeline still runs end to end.

ANTHROPIC_API_KEY=

# Optional overrides (defaults shown)
INSIGHT_MODEL=claude-sonnet-4-6
